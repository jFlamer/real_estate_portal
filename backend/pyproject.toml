[project]
name = "smart-listings-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "pymysql",
    "cryptography",
    "pydantic-settings",
    "httpx",
    "beautifulsoup4",
    "google-genai",
]

[dependency-groups]
dev = ["pytest"]